# Loops in Python

#1: For loops
# Every time for loop runs it increases by 1 and runs from o all the way to 9 (Excludes 10)
for x in range(0, 10, 2): # Start Stop Step (Only works for integer values else you have to install NumPy)
    print(x)